# Blakhole V3.0.0 更新说明

## 版本信息

- 版本：V3.0.0
- 基础版本：V2.0.0
- 适用平台：Windows x64

## 新增功能

### 鼠标点击波纹

高级设置中新增“鼠标点击波纹”模式：

- 常驻关闭（默认）
- 常驻开启
- 仅黑洞实际显示时开启

波纹由鼠标点击触发，具有扩散、折射、波峰高光和自然衰减效果。仅黑洞实际显示时开启的模式不会因为后台进程存在而显示特效。

### 吸积盘渲染模式

新增全局吸积盘渲染选择：

- 柔化（默认）：延续 V2.0.0 的柔化效果
- 线条化：提高原有线条纹理对比度
- 边缘柔滑：保留线条感，同时柔化原线条并增加外侧过渡区域

设置同时作用于主渲染器、吞噬渲染和 UI 预览。

## 保留内容

- 保留 V2.0.0 吞噬特效
- 保留原有多屏、预设、捕获和配置功能
- 新配置缺失时自动使用默认模式，不影响旧配置文件

## 发布包说明

正式发布包 `BlakholeUI-v3.0.0-package.zip` 包含完整运行文件、Qt 运行库、渲染 Shader、默认配置和 UI 资源，并额外包含项目收款码图片，供发布页面展示或支持作者使用。

代码仓库不包含收款码图片；收款码仅随发布包提供。

## 验证记录

- Renderer CTest：5/5 通过
- Qt UI CTest：9/9 通过
- GPU 波纹折射、衰减和恢复测试通过
- V2.0.0 吞噬特效回归通过
- 吸积盘三种模式和 UI 预览测试通过
