# Black Hole v1.2.4

本次版本集中修复多显示器、显卡驱动和桌面捕获兼容问题。

## 修复内容

- 修复 AMD 等严格 OpenGL 驱动报告 `gl_FragColor` 保留标识符，导致片元 Shader 编译失败的问题。
- 修复启用吸积盘光影后，桌面左下区域被放大到全屏的问题。
- 改用 Per-Monitor V2 DPI 感知，改善主副屏缩放比例不同时的窗口尺寸、位置和跨屏显示异常。
- 为每个渲染目标增加单实例限制，避免重复启动 Renderer 造成黑洞和桌面画面叠加。
- 黑洞移动速度最低值改为 0，设置为 0 时可保持黑洞静止。
- 修复 Windows 11 WGC 捕获帧释放过早造成的 GPU 并发竞争，降低闪烁、旧帧和不稳定现象。

## 已知限制

- 吸积盘光影仍是实验性功能。启用后可能出现泛白、对比度变化或其他画面异常，本版不调整该部分算法。
- 混合 DPI 多屏修复已通过代码、构建和单实例测试，但仍建议使用不同缩放比例双屏的用户重点复测跨屏、一屏一黑洞和仅副屏模式。
- 程序未使用商业代码签名，少数安全软件仍可能误报；请只从本仓库 Release 下载并核对 SHA-256。

## 安装

下载 `BlakholeUI-v1.2.4-windows-x64.zip` 后完整解压，再运行 `appBlakholeUI.exe`。不要只替换单个 EXE。

最终 ZIP 的 SHA-256 以同名 `.sha256.txt` 校验文件和 GitHub Release 页面为准。
