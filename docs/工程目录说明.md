# 工程目录说明

## 当前源码
- 根目录 main 已合并 codex/desktop-consumption，V2.0.0 对应 3281489。
- src、shaders、Blakhole_UI、cmake、CMakeLists.txt 和 blackhole.glsl 与该标签一致。
- 移动、光影和 v1.2.4 兼容性历史分支已包含在 V2.0.0 中；保留分支和已有工作树，避免丢失本地文件。
- codex/mouse-ripple-disk-modes 为未完成且基线错误的实验分支，未并入生产源码。

## 当前构建
- build/current-renderer：从根目录源码全新配置的 Renderer 构建及测试。
- build/current-ui：从 Blakhole_UI 全新配置的 UI 构建，已配套同次构建 Renderer、Shader 和运行配置副本。
- 当前两份 EXE 版本资源均为 2.0.0.0。Renderer CTest 3/3 通过，UI 编译成功；UI 保留既有 QImage 弃用警告。

## 历史资料
- build/history-v124：从根目录移入的三个 _build_v124 构建目录和两个 _verify_v124 验证目录；批量删除被自动审批拦截，因此仅归档，未释放磁盘空间。
- build 下其余候选目录、release 和 release-v1.2.2-backup 均是历史产物，目录名不能作为版本证据。
- .worktrees 包含真实 Git 工作树及本地文件，不应作为普通缓存删除。
- 发布 ZIP、说明文档、根目录用户配置和资源图片保留。
- 整理前用户两份修改配置已恢复，另有命名 stash 备份。未推送远端。

## 后续功能
鼠标点击波纹和吸积盘三模式尚未完成。此前旧构建缓存指向其他工作树，相关编译验证结论撤回。后续应基于本次已验证的 V2.0.0 源码实施。
